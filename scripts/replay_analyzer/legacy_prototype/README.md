# Legacy replay prototype quarantine

Every file below this directory is historical, unverified, and non-factual.
The prototype is excluded from the `generals-replay-analyzer` package and must
not be imported by production code or tests.

These files may be deleted only after any potentially reusable rendering,
reporting, camera, or casting utilities have been separately revalidated
against authoritative engine telemetry and moved into a trusted package.
